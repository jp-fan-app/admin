#extend("base/fullscreen"):
    #export("title", "Dashboard")
    #export("bodyClass", "")
    #export("body"):
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/models">Models</a></li>
                <li class="breadcrumb-item"><a href="/models/#(context.model.id)">#(context.model.name)</a></li>
                <li class="breadcrumb-item">#(context.image.id)</li>
                <li class="breadcrumb-item active">Set Main Image</li>
            </ol>
        </nav>

        <div class="row justify-content-md-center">
            <div class="col col-lg-6">
                <form method="POST">
                    <h2>Confirmation</h2>
                    <p>Do you really want set image <b>#(context.image.id)</b> as main image for <b>#(context.model.name)</b></p>
                    <button type="submit" class="btn btn-primary">Set Main Image</button>
                </form>
            </div>
        </div>

    #endexport
#endextend
